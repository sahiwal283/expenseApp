/**
 * Shared Hooks
 * 
 * Centralized exports for all app-wide shared hooks.
 * These hooks can be used across any component in the application.
 * 
 * Usage:
 * import { useUsers, useApiError } from '@/hooks';
 */

export { useUsers } from './useUsers';
export { useApiError } from './useApiError';
export { useResourceLoader } from './useResourceLoader';

