# Production Environment Configuration Template
# Copy this file to /etc/expenseapp-prod/backend.env on production server
# Fill in all values marked with <placeholder>
# NEVER commit actual secrets to Git

# ===========================================
# APPLICATION CONFIGURATION
# ===========================================
NODE_ENV=production
PORT=3000

# ===========================================
# SECURITY SECRETS (GENERATE NEW FOR PRODUCTION!)
# ===========================================
# Generate with: openssl rand -base64 32
JWT_SECRET=<GENERATE_NEW_SECRET_HERE>
SESSION_SECRET=<GENERATE_NEW_SECRET_HERE>

# ===========================================
# DATABASE CONFIGURATION (PRODUCTION)
# ===========================================
DB_HOST=<production_db_host>
DB_PORT=5432
DB_NAME=expenseapp_production
DB_USER=<production_db_user>
DB_PASSWORD=<production_db_password>

# ===========================================
# FRONTEND CONFIGURATION
# ===========================================
CORS_ORIGIN=https://expenseapp.yourdomain.com
FRONTEND_URL=https://expenseapp.yourdomain.com

# ===========================================
# ZOHO BOOKS API - PRODUCTION CREDENTIALS
# ===========================================
# CRITICAL: These MUST be different from sandbox credentials
# Obtain from Zoho Books API Console for PRODUCTION organization

# Haute Entity (Production - REAL API)
ZOHO_CLIENT_ID=<production_client_id>
ZOHO_CLIENT_SECRET=<production_client_secret>
ZOHO_REFRESH_TOKEN=<production_refresh_token>
ZOHO_ORGANIZATION_ID=<production_organization_id>

# Zoho Account IDs (from production Chart of Accounts)
ZOHO_EXPENSE_ACCOUNT_ID=<production_expense_account_id>
ZOHO_PAID_THROUGH_ACCOUNT_ID=<production_paid_through_account_id>

# Zoho API Base URLs (standard, should not change)
ZOHO_API_BASE_URL=https://www.zohoapis.com/books/v3
ZOHO_ACCOUNTS_BASE_URL=https://accounts.zoho.com/oauth/v2

# Account Names (fallback if IDs not available)
ZOHO_EXPENSE_ACCOUNT_NAME=Meals
ZOHO_PAID_THROUGH_ACCOUNT=Business Checking

# ===========================================
# MULTI-ENTITY ZOHO CONFIGURATION (OPTIONAL)
# ===========================================
# Enable additional entities only if you have production credentials for them
# Set MOCK=false only when real credentials are available

# Alpha Entity
ZOHO_ALPHA_ENABLED=false
ZOHO_ALPHA_MOCK=true

# Beta Entity
ZOHO_BETA_ENABLED=false
ZOHO_BETA_MOCK=true

# Gamma Entity
ZOHO_GAMMA_ENABLED=false
ZOHO_GAMMA_MOCK=true

# Delta Entity
ZOHO_DELTA_ENABLED=false
ZOHO_DELTA_MOCK=true

# ===========================================
# FILE UPLOAD CONFIGURATION
# ===========================================
MAX_FILE_SIZE=10485760
UPLOAD_DIR=/var/lib/expenseapp/uploads

# ===========================================
# LOGGING CONFIGURATION
# ===========================================
LOG_LEVEL=info

# ===========================================
# IMPORTANT NOTES
# ===========================================
# 1. Never use sandbox credentials in production
# 2. Generate new JWT_SECRET for production
# 3. Use strong database passwords
# 4. Verify Zoho Organization ID matches production org
# 5. Set file permissions to 600: chmod 600 /etc/expenseapp-prod/backend.env
# 6. Restart service after changes: systemctl restart expenseapp-backend

